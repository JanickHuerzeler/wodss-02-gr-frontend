import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

xdescribe("test suite works", () => {
  test('dummy test', ()=>{
    expect(1).toBe(1);
  })
});
